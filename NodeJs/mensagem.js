const boasvindas = (nome) => {
    return `Seja bem vindo, ${nome}`;
}

const despedida = (nome) => {
    return `Até logo, ${nome}`;
}

const elogio = (nome) => {
    return `Você eh mto legal, ${nome}`
}

module.exports = {boasvindas, despedida, elogio}